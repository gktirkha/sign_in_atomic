import 'package:flutter/material.dart';

import '../atom/center_text.dart';

class Template5 extends StatelessWidget {
  const Template5({super.key});

  @override
  Widget build(BuildContext context) {
    return const CenterText(num: 5);
  }
}
