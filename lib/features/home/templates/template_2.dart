import 'package:flutter/material.dart';

import '../atoms/center_text.dart';

class Template2 extends StatelessWidget {
  const Template2({super.key});

  @override
  Widget build(BuildContext context) {
    return const CenterText(num: 2);
  }
}
