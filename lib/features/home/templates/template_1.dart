import 'package:flutter/material.dart';

import '../atoms/center_text.dart';

class Template1 extends StatelessWidget {
  const Template1({super.key});

  @override
  Widget build(BuildContext context) {
    return const CenterText(num: 1);
  }
}
