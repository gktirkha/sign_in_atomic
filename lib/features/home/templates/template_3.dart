import 'package:flutter/material.dart';

import '../atom/center_text.dart';

class Template3 extends StatelessWidget {
  const Template3({super.key});

  @override
  Widget build(BuildContext context) {
    return const CenterText(num: 3);
  }
}
